<%- include ('../../header.ejs') %>
<div id="container">
    <%- include ('../nav.ejs') %>
    <div id="content-row">
        <div id="list-container">
            <h1><%= title %></h1>
            <a id="upload-word-link" href="/cms/lists/upload">Create a new list</a>
            <% if(typeof (lists) != "undefined") { %>
                <ul>
                    <% lists.forEach(list => { %>
                        <li>
                            <a class="square-link" href="lists/<%= list.id %>">
                                <%= list.list %>
                            </a>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
        </div>
    </div>
    <style>
        #container {
            display: flex;
            flex-direction: row;
            height: 100%;
        }

        #content-row {
            display: flex;
            width: 100%;
            flex-direction: row;
            flex-wrap: wrap;
            padding: 16px 16px 16px 32px;
        }

        #content-row > div {
            flex: 1;
            padding-right: 32px;
        }

        #upload-word-link {
            display: block;
            background-color: #4CAF50;
            color: white;
            text-align: center;
            padding: 32px;
            text-decoration: none;
            border-radius: 12px;
        }
    </style>

    <script>
        const {Client, Account, ID} = Appwrite;
        const client = new Client()
            .setEndpoint("https://aw.responsible-it.nl/v1")
            .setProject("63a32dec14be845ec0f9")

    </script>
</div>
<%- include ('../../footer.ejs') %>