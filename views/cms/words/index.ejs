<%- include ('../../header.ejs') %>
    <%- include ('../nav.ejs') %>
        <div id="words-container">
            <h1><%= title %></h1>
            <a id="upload-word-link" href="/cms/words/upload">Create a new word</a>
            <% if(typeof (words) != "undefined") { %>
                <ul>
                    <% words.forEach(word => { %>
                        <li>
                            <a class="square-link" href="words/<%= word.id %>">
                                <%= word.word %>
                            </a>
                        </li>
                    <% }); %>
                </ul>
            <% } %>
    </div>
    <style>
        #content-row > div {
            flex: 1;
            padding-right: 32px;
        }

        #upload-word-link {
            display: block;
            background-color: #4CAF50;
            color: white;
            text-align: center;
            padding: 32px;
            text-decoration: none;
            border-radius: 12px;
        }
    </style>

    <script>
        const {Client, Account, ID} = Appwrite;
        const client = new Client()
            .setEndpoint("https://aw.responsible-it.nl/v1")
            .setProject("63a32dec14be845ec0f9")

    </script>
</div>
<%- include ('../../footer.ejs') %>